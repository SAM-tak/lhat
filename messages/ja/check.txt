# check -- L^ 0.3.3 の英語から訳した。
# none = no error
none = 誤りなし
# undefined = no such name in scope: {name}
undefined = スコープにこの名前は無い: {name}
# used-before-defined = this name is read before its let^ has run
used-before-defined = この名前は、その let^ が走る前に読まれている
# redefined = this name is already defined in this scope: {name}
redefined = この名前はこのスコープで既に定義されている: {name}
# unknown-type = no such type
unknown-type = そのような型は無い
# mismatch = this value does not fit where it is written
mismatch = この値は、書かれた場所に合わない
# shape-refused = this call hands over argument types the body cannot take; writing the parameter types is what would surface the body's own report
shape-refused = この呼び出しが渡す引数の型を、本体は受け取れない。仮引数の型を書けば、本体自身の報告が出る
# not-number = arithmetic needs number^
not-number = 算術には number^ が要る
# not-bool = this has to be bool^
not-bool = ここは bool^ である必要がある
# match-not-exhaustive = a match written as an expression answers in every case, and these arms do not cover every value the subject may hold; write 'other^: ...' before the ';'
match-not-exhaustive = 式として書いたパターンマッチはどの場合にも答えるが、これらの選択肢は対象が持ちうる値をすべては覆っていない。';' の前に 'other^: ...' を書く
# not-callable = this is not a function or a procedure
not-callable = これは関数でも手続きでもない
# function-calls-procedure = f^ may call only f^, and this callee is a p^
function-calls-procedure = f^ が呼べるのは f^ だけで、この呼び先は p^ である
# arity = the wrong number of arguments
arity = 引数の数が違う
# not-variadic = '...' spreads into a variadic tail, and this callee takes none
not-variadic = '...' は可変長の末尾へ展開するが、この呼び先は可変長の末尾を取らない
# no-member = this value has no such member
no-member = この値にそのようなメンバは無い
# no-result-type = this signature answers no value, so it has no ReturnType
no-result-type = この署名は値を答えないので、ReturnType を持たない
# kind-as-value = a kind is a type, not a value; error^Kind{ ... } is what makes one: {kind}
kind-as-value = 種別は型であって値ではない。値を作るのは error^Kind{ ... } である: {kind}
# cannot-fail = the left of catch^ or try^ cannot return an error
cannot-fail = catch^ や try^ の左辺は誤りを返しえない
# cannot-be-nil = the left of ?? cannot be nil^
cannot-be-nil = ?? の左辺は nil^ になりえない
# try-outside = try^ would return an error this subroutine cannot return
try-outside = try^ が返す誤りを、このサブルーチンは返せない
# local-error-escapes = this error has to be resolved here: a localerror^ is not one a subroutine may return. Write catch^, or wrap the statements in try^{ } with an arm that takes it
local-error-escapes = この誤りはここで解決する必要がある。localerror^ はサブルーチンが返してよい誤りではない。catch^ を書くか、それを受ける選択肢を持つ try^{ } で文を囲む
# local-error-written = a localerror^ cannot be written where a caller would receive it -- not in a result, a yield, or a declared field
local-error-written = localerror^ は呼び手が受け取る場所には書けない。結果にも、yield にも、宣言したフィールドにも
# never-returns = every way out of this body calls it again, so it never produces a value
never-returns = この本体から出る道はどれも自分を呼び直すので、値を作ることが無い
# result-undecided = the result type did not come out of this body, so it has to be written
result-undecided = 結果の型がこの本体から決まらなかったので、書く必要がある
# param-undecided = nothing in this body says what this parameter is, so its type has to be written; any^ is how to say it really does take anything
param-undecided = この仮引数が何かを本体の中の何も言っていないので、型を書く必要がある。本当に何でも取るなら any^ と書く
# type-undecided = inference did not decide what this name holds, so its type has to be written
type-undecided = 推論はこの名前が何を持つかを決めなかったので、型を書く必要がある
# function-falls-out = a function answers on every path; this one has a path that leaves without a value
function-falls-out = 関数はどの経路でも答える。この関数には値を持たずに抜ける経路がある
# falls-out-of-result = this body has a path that leaves without a value, which the result type it was given does not admit
falls-out-of-result = この本体には値を持たずに抜ける経路があり、与えられた結果の型はそれを許さない
# this-outside = this^ names the subroutine running, and none is here
this-outside = this^ は走っているサブルーチンを指すが、ここには無い
# super-outside = super^ is the member an override^ replaces, so it is a name only inside one
super-outside = super^ は override^ が置き換えるメンバなので、その中でだけ名前になる
# not-disposable = with^ needs a value with a dispose() that returns nothing
not-disposable = with^ には、何も返さない dispose() を持つ値が要る
# discard-read = '_^' throws the value away, so it is not a name and there is nothing here to read; write a name where the value is wanted
discard-read = '_^' は値を捨てるので名前ではなく、ここで読める物は無い。値が要る所には名前を書く
# error-dropped = this can fail, and dropping the answer drops the failure with it; write try^ to hand it back, catch^ to answer instead, or a name to bind it and narrow
error-dropped = これは失敗しうるので、答えを捨てれば失敗も一緒に捨てることになる。返すなら try^、代わりに答えるなら catch^、束縛して絞り込むなら名前を書く
# member-exists = this name is already a member; write override^ or overload^
member-exists = この名前は既にメンバである。override^ か overload^ を書く
# compose-collides = both definitions carry a member of this name, and a marker can only be written inside a def^: {member}
compose-collides = どちらの定義もこの名前のメンバを持ち、印は def^ の中でしか書けない: {member}
# concat-collides = both tables carry this key, so the concatenation holds no one value for it
concat-collides = どちらのテーブルもこの鍵を持つので、連結した結果はそれにひとつの値を持たない
# ambiguous-member = both sides of the composition carry this name, so it reaches no one answer; name the side you mean
ambiguous-member = 合成の両側がこの名前を持つので、ひとつの答えに届かない。どちらの側かを名指す
# still-abstract = this definition is still waiting on a composition -- a member is declared with nothing providing it, or an override^ has met nothing to replace: {member}
still-abstract = この定義はまだ合成を待っている。宣言だけで何も与えていないメンバがあるか、override^ が置き換える物に出会っていない: {member}
# field-unprovided = this field is declared with no value, so an instance would hold nothing under it; a composition has to give it one, or an override^ new has to write it: {field}
field-unprovided = このフィールドは値の無いまま宣言されているので、インスタンスはそこに何も持たない。合成が値を与えるか、override^ new が書く必要がある: {field}
# lent-does-not-provide = this member is declared with abstract^, and what a delegate^ lends does not provide it -- to use the delegate's, drop the abstract^; otherwise compose something that provides it: {member}
lent-does-not-provide = このメンバは abstract^ で宣言されていて、delegate^ が貸す物はそれを与えない。委譲先の物を使うなら abstract^ を外し、そうでなければそれを与える物を合成する: {member}
# lent-is-not-replaced = this override^ has nothing to replace -- what a delegate^ lends is not a member to replace, so super^ would reach nothing; compose what it replaces, or drop the override^: {member}
lent-is-not-replaced = この override^ には置き換える物が無い。delegate^ が貸す物は置き換えるメンバではないので、super^ は何にも届かない。置き換える物を合成するか、override^ を外す: {member}
# already-provided = something in the chain already provides this member, so there is nothing for an abstract^ to ask for
already-provided = 連鎖の中の何かが既にこのメンバを与えているので、abstract^ が求める物は無い
# abstract-provided-here = an abstract^ asks a composition for what this def^ does not have, and this one is written here as well; the members reach each other whatever the order, so drop the declaration
abstract-provided-here = abstract^ はこの def^ に無い物を合成に求めるが、この物はここにも書かれている。メンバは順序に関係なく互いに届くので、宣言を外す
# mutable-default = a field's default lives on the prototype: an immutable value, a table written out as a literal (each instance is given its own copy), or a definition (shared by design); what something else made -- a name, a call's answer -- is given inside new: {field}
mutable-default = フィールドの既定値はプロトタイプに置かれる。不変の値、リテラルとして書いたテーブル（インスタンスごとに複製が渡る）、または定義（意図して共有される）のいずれかである。ほかで作った物（名前、呼び出しの答え）は new の中で与える: {field}
# new-returns = construction answers the instance itself, so a new body has no return^; what it writes through self^ is already on what the caller gets
new-returns = 構築はインスタンスそのものを答えるので、new の本体は return^ を持たない。self^ を通して書いた物は、既に呼び手が受け取る物の上にある
# self-table-outside-new = self^{ ... } is the construction notation, which only a written new runs; a method writes its receiver one field at a time, through self^.name
self-table-outside-new = self^{ ... } は構築の記法で、書かれた new だけが走らせる。メソッドはレシーバを self^.name を通してひとつずつ書く
# prototype-sealed = a definition's self^ is the prototype every instance starts as; it is read here and written by no one -- a default is settled where the field is written
prototype-sealed = 定義の self^ は、どのインスタンスも最初にそれとして始まるプロトタイプである。ここでは読まれるだけで誰も書かない。既定値はフィールドを書いた所で決まる
# mutable-key = a key's hash is the box's bytes, and only a sealed box keeps them still; write constbox^ to make one
mutable-key = 鍵のハッシュは箱のバイト列で、それを動かさずに保つのは封印された箱だけである。constbox^ と書いて作る
# box-field-write = a box's field is read straight off its bytes; writing goes through set(...), which a ConstBox^ does not have
box-field-write = 箱のフィールドはそのバイト列から直接読まれる。書くのは set(...) を通してで、ConstBox^ はそれを持たない
# not-boxable = box^ takes a host value; everything else already lives on the heap and needs no box
not-boxable = box^ が取るのはホスト値である。それ以外は既にヒープにあり、箱は要らない
# nothing-to-override = there is no member of this name to override^ or overload^
nothing-to-override = override^ や overload^ の対象になる、この名前のメンバは無い
# not-substitutable = override^ has to be usable where the original was
not-substitutable = override^ は、元の物が使えた場所で使える必要がある
# overload-overlaps = overload^ overlaps an existing signature
overload-overlaps = overload^ が既存の署名と重なっている
# operator-unsettled = several types here carry this operator and nothing says which is meant; write one of the types the signature names, or narrow to it with fits^
operator-unsettled = ここではいくつもの型がこの演算子を持ち、どれのことかを何も言っていない。署名が名指す型のひとつを書くか、fits^ でそれに絞り込む
# incomparable = these can never be equal, so the comparison is fixed already
incomparable = これらは決して等しくならないので、比較の答えは既に決まっている
# as-impossible = nothing is both of these, so this as^ could never succeed
as-impossible = この両方である物は無いので、この as^ は決して成功しない
# bad-key = nil^ is how a table spells 'not there', so it cannot be a key
bad-key = nil^ はテーブルで「無い」を綴る値なので、鍵にはなれない
# no-operator = an operator is answered by what stands to its left, or by what stands to its right when that side writes the self^ last; neither answers this one
no-operator = 演算子に答えるのは左に立つ物か、右側が self^ を最後に書くならその右に立つ物である。どちらもこの演算子に答えない
# bad-operator = an op^ is an f^ taking self^ and one argument, and it may not yield^; the self^ is whichever operand it is written as -- first for the left one, last for the right. Only op^- is also written with self^ alone, which is the unary one
bad-operator = op^ は self^ とひとつの引数を取る f^ で、yield^ してはならない。self^ は書かれた位置の被演算子で、左なら最初、右なら最後に書く。self^ だけで書く op^ は op^- だけで、それが単項のものである
# isa-always-true = any^ holds of every value, so this asks nothing
isa-always-true = any^ はどの値にも当てはまるので、これは何も問わない
# missing-field = this field has no default, so it has to be written
missing-field = このフィールドには既定値が無いので、書く必要がある
# require-failed = this unit could not be required
require-failed = この単位を require^ できなかった
# coroutine-dropped = this call makes a coroutine and runs none of the body; write await^ to delegate, or let^ to keep it
coroutine-dropped = この呼び出しはコルーチンを作るだけで、本体を何も走らせない。委譲するなら await^、取っておくなら let^ と書く
# not-coroutine = this has no coroutine to walk or delegate to
not-coroutine = これには、走査したり委譲したりするコルーチンが無い
# await-not-coroutine = await^ waits for something that finishes, and this is not one -- what may be awaited is a coroutine (15.14)
await-not-coroutine = await^ は終わる物を待つが、これはそうではない。待てるのはコルーチンである（15.14）
# coroutine-mismatch = the coroutine this body makes is not the one written here; the yield^ sites and the c^{ … } have to agree
coroutine-mismatch = この本体が作るコルーチンは、ここに書かれた物と違う。yield^ の箇所と c^{ … } が一致する必要がある
# yield-needs-annotation = a yield^ that is bound needs a written type there
yield-needs-annotation = 束縛する yield^ には、そこに型を書く必要がある
# yield-type-mismatch = every yield^ in one body has to agree on what it sends and what it answers
yield-type-mismatch = ひとつの本体の中の yield^ は、送る物と答える物がすべて一致する必要がある
# phantom-yield-binds = a _yield^ never runs, so nothing arrives here; bind _^ -- the annotations are all this statement says
phantom-yield-binds = _yield^ は走らないので、ここには何も届かない。_^ を束縛する。この文が言うのは型の注記だけである
# not-indexable = this is not a table, so a key reaches nothing; a string^'s characters answer at(i)
not-indexable = これはテーブルではないので、鍵は何にも届かない。string^ の文字は at(i) が答える
# path-not-table = this holds the name written after it, so it has to be a table
path-not-table = これは後に書かれた名前を持つので、テーブルである必要がある
# path-is-definition = a def^ says what its instances carry, so a member cannot be added to one here
path-is-definition = def^ はそのインスタンスが持つ物を言うので、ここでメンバを足すことはできない
# module-unnamed = this unit declares no module^, so there is no path to bind it under; write 'let^ name = require^ ...' instead
module-unnamed = この単位は module^ を宣言していないので、束縛するパスが無い。代わりに 'let^ name = require^ ...' と書く
# not-hosted = import^ reaches what the host registered, and no module of this name is there; a unit read from a file comes in with require^ "path"
not-hosted = import^ はホストが登録した物に届くが、この名前のモジュールはそこに無い。ファイルから読む単位は require^ "path" で入れる
# scope-too-far = this reaches out past more scopes than are open here
scope-too-far = ここで開いているより多くのスコープの外へ届こうとしている
# scope-on-define = let^ makes a name here, so it takes no scope specifier; ':=' is what writes one that is already there
scope-on-define = let^ はここで名前を作るので、スコープ指定子を取らない。既にある名前を書くのは ':=' である
# function-writes-out = an f^ assigns to local variables only, and this name was bound outside its body
function-writes-out = f^ が代入できるのは局所変数だけで、この名前は本体の外で束縛されている
# function-changes-table = an f^ may change only a table its own body made; this one came from somewhere else
function-changes-table = f^ が変えられるのは自分の本体が作ったテーブルだけで、これはほかから来た
# path-is-opaque = this type carries what registered it, so what it holds cannot be written over
path-is-opaque = この型はそれを登録した物を持つので、中身を上書きできない
# advances-outside = an f^ may advance only a coroutine its own body made; this one came from somewhere else
advances-outside = f^ が進められるのは自分の本体が作ったコルーチンだけで、これはほかから来た
# mutates-outside = an f^ may write through mutable^self^ only into a table its own body made; this one came from somewhere else
mutates-outside = f^ が mutable^self^ を通して書けるのは自分の本体が作ったテーブルだけで、これはほかから来た
# answer-not-fresh = a fresh^ answer has to be made by this body -- a literal, a new(), or another fresh^ call's answer
answer-not-fresh = fresh^ の答えはこの本体が作った物である必要がある。リテラル、new()、または別の fresh^ 呼び出しの答え
# coroutine-escapes = an f^ coroutine may not leave the body that made it
coroutine-escapes = f^ のコルーチンは、それを作った本体から出てはならない
# table-is-sealed = this table belongs to the machine; what it holds is written by the host, not from here
table-is-sealed = このテーブルは機械のものである。中身を書くのはホストであり、ここからではない
# assign-to-let = this name was bound by a let^ and is not reassigned; write var^ where the name has to change: {name}
assign-to-let = この名前は let^ で束縛されていて、代入し直さない。名前を変える必要があるなら var^ と書く: {name}
# assign-to-form = the construct that introduces this name is what gives it a value -- a with^ holds it for the block, a for^ advances or rebinds its focus -- so nothing else writes it: {name}
assign-to-form = この名前を導く構文が値を与える物である。with^ はブロックの間それを持ち、for^ はその焦点を進めるか束縛し直す。ほかの何もそれを書かない: {name}
# public-is-immutable = a public^ declaration binds with let^; another unit would otherwise see a name change under it
public-is-immutable = public^ の宣言は let^ で束縛する。そうでなければ、ほかの単位から名前が足元で変わって見える
# compare-not-number = op^<=> answers a number^: '<' and the rest read which side of zero the answer falls on
compare-not-number = op^<=> は number^ で答える。'<' などは、その答えがゼロのどちら側かを読む
# equal-not-bool = op^= answers a bool^: '=' takes it as it stands, and '≠' negates it
equal-not-bool = op^= は bool^ で答える。'=' はそれをそのまま使い、'≠' はそれを否定する
# not-ordered = nothing here says how these compare: an ordering is read off '<=>', and neither side carries one that takes the other
not-ordered = これらをどう比べるかを何も言っていない。順序は '<=>' から読むが、どちらの側も相手を取るものを持たない
# self-last-not-operator = only an op^ writes its self^ last, to say the right operand is the receiver; everywhere else the receiver is what stands before the dot
self-last-not-operator = self^ を最後に書くのは op^ だけで、右の被演算子がレシーバだと言うためである。ほかではレシーバはドットの前に立つ物である
# self-type-outside = Self^ names the t^ or def^ written around it, and there is no such literal here -- or a second hat counted past the outermost one
self-type-outside = Self^ はそれを囲んで書かれた t^ か def^ を指すが、ここにはそのリテラルが無い。または2つ目のハットが一番外を越えて数えた
# tuple-misplaced = (A, B) is what a subroutine answers with, and it is written nowhere else -- not as an argument, a name, a table member, or a position of another tuple; pack^ makes a t^{ A, B } of one
tuple-misplaced = (A, B) はサブルーチンが答える形で、ほかのどこにも書かない。引数にも、名前にも、テーブルのメンバにも、別のタプルの位置にも。pack^ がそれから t^{ A, B } を作る
# tuple-union = the only thing (A, B) may be written in a union with is an error
tuple-union = (A, B) と合併にして書けるのは誤りだけである
# hostvalue-union = a host value takes several slots, so what stands beside it in a union has to be told apart by the first of them -- nil^ or an error, the same as beside (A, B)
hostvalue-union = ホスト値はいくつものスロットを取るので、合併でその横に立つ物は最初のスロットで見分けられる必要がある。(A, B) の横と同じく nil^ か誤りである
# tuple-arity = this answers a different number of values than there are names to take them
tuple-arity = これが答える値の数が、受け取る名前の数と違う
# tuple-error-position = an error goes around the values, not among them; write (A, B)|SomeError rather than (A, SomeError)
tuple-error-position = 誤りは値の周りに置き、値の間には置かない。(A, SomeError) ではなく (A, B)|SomeError と書く
# catches-nothing = nothing in this try^{ } can fail: an error reaches the arms by being written try^, and none is
catches-nothing = この try^{ } の中に失敗しうる物が無い。誤りは try^ と書かれて選択肢に届くが、そう書かれた物が無い
# closed-captures = a closed^ body names nothing standing outside it: pass this as an argument instead. An import^ed module, a name the host bound, and L^ are reached without capturing and may be written here: {name}
closed-captures = closed^ の本体は外に立つ物を名指さない。これは引数として渡す。import^ したモジュール、ホストが束縛した名前、L^ は捕捉せずに届くので、ここに書いてよい: {name}
# hostvalue-escapes = a host value lives on the stack and nowhere else; box it into the container type its library provides to keep it
hostvalue-escapes = ホスト値はスタックにだけあり、ほかのどこにも無い。取っておくなら、そのライブラリが与える容器の型に箱詰めする
# operator-on-maybe-nil = this may be nil^, and nil^ answers no operator; '??' gives it a value, or bind it to a name and narrow that -- an index is not narrowed where it stands
operator-on-maybe-nil = これは nil^ でありうるが、nil^ はどの演算子にも答えない。'??' で値を与えるか、名前に束縛してそれを絞り込む。添え字はその場では絞り込まれない
# no-such-annotation = no host registered an annotation of this name: {annotation}
no-such-annotation = この名前の注釈を登録したホストは無い: {annotation}
# annotation-misplaced = this annotation was not registered for what it is written above: {annotation}
annotation-misplaced = この注釈は、上に書かれた物に対しては登録されていない: {annotation}
# annotation-arguments = these are not the arguments the annotation was registered with: {annotation}
annotation-arguments = これは、この注釈が登録された引数ではない: {annotation}
# annotation-exclusive = this annotation and another written in this file are two answers to one question: write whichever one applies, not both: {annotation}
annotation-exclusive = この注釈と、このファイルに書かれた別の注釈は、ひとつの問いへの二つの答えである。当てはまる方だけを書き、両方は書かない: {annotation}
# annotation-requisite = this annotation means nothing on its own, and the one it has to stand beside is missing: {annotation}
annotation-requisite = この注釈は単独では意味を持たず、横に立つべき注釈が無い: {annotation}
# annotation-repeated = this annotation may be written once in a file, and already was: {annotation}
annotation-repeated = この注釈はファイルに一度しか書けず、既に書かれている: {annotation}
# bare-table-type = a t^ is written with the members it asks for, and the top of tables asks for none: write 't^{}'
bare-table-type = t^ は求めるメンバと一緒に書く。テーブルの最上位の型は何も求めない。't^{}' と書く
# mismatch.named = this value does not fit where it is written: {member}
mismatch.named = この値は、書かれた場所に合わない: {member}
# no-member.named = this value has no such member: {member}
no-member.named = この値にメンバ {member} は無い
# ambiguous-member.named = both sides of the composition carry this name, so it reaches no one answer; name the side you mean: {member}
ambiguous-member.named = 合成の両側がこの名前を持つので、ひとつの答えに届かない。どちらの側かを名指す: {member}
# not-substitutable.named = override^ has to be usable where the original was: {member}
not-substitutable.named = override^ は、元の物が使えた場所で使える必要がある: {member}
